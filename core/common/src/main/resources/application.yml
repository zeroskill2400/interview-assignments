server:
  port: 18080

spring:
  application:
    name: common

  # JPA
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/db
    username: zeroskill2400
    password: Zeroskill2400
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
#    hibernate:
#      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: supersecretkey